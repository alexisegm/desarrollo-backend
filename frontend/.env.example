# Copy to .env — the URL of YOUR running backend (class 05 API).
# Remember: the backend's FRONTEND_ORIGIN must be this app's origin
# (with the default Vite dev server: http://localhost:5173).

VITE_API_URL=http://localhost:3000